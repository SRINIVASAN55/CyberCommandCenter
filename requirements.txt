rich>=13.0.0
scikit-learn>=1.0.0
numpy>=1.21.0
requests>=2.28.0

# All packages are OPTIONAL — the tool runs on Python stdlib alone.
# Install for enhanced features:
#   rich         → Beautiful terminal dashboard UI
#   scikit-learn → ML-powered Isolation Forest anomaly detection
#   numpy        → Numerical feature processing
#   requests     → Live AbuseIPDB threat intel lookups